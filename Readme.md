*This is the tutorial files to make the good understanding on the merging concept of github*
if you created a branch on the local repo then you also have to push it to the repo
use the command -> git push -u origin branch_name

if you have created a new branch from the github account  you also have to fetch that branch 
use the command -> git fetch 

